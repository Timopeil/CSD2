function(t) sin(t*2*pi)

sin((t*2*pi)/48000)

sin( (t*freq*2*pi)/48000 )

1 = 1/4
0 = 0 , 1/2, 1
-1 = 3/4

fase += freq / samplerate


Eindopdracht:
  Inhertance
  Sine wave
  (polymorphism)
  (yomamma)


Maak microtonal synth dat lijkt op een whatever. &eindopdrach

Run files:
g++ -c file.cpp
g++ file.o
chmod a+x ./a.out
./a.out

jackd -R -d net -a $MASTER_IP
